databaseChangeLog:

  - changeSet:
      id: DEV-6
      author: vlad
      changes:
        - addColumn:
            tableName: users
            columns:
              - column:
                  name: online_date
                  type: timestamp without time zone
                  defaultValue: now()
                  constraints:
                    nullable: false